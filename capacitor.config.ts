import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mycapapp.app",
  appName: "my-pwa-app",
  webDir: "build",
  bundledWebRuntime: true,
};

export default config;
